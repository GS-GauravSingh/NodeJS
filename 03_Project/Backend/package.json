{
  "name": "user_manager_api",
  "version": "1.0.0",
  "description": "\"User Manager API\" is a simple RESTful API that allows you to manage user data. It supports creating, reading, updating, and deleting (CRUD) operations, with data stored in a MongoDB database. This API provides endpoints to fetch all users, retrieve individual users by ID, add new users, update existing user details, and delete users.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js"
  },
  "author": "Gaurav Singh",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "nodemon": "^3.1.7"
  }
}
